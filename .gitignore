.env
data/
data-sim/
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.jsonl

# Recorder supervisor runtime artifacts (restart log, pid of the running
# supervisor). Local to one machine's run; the data itself is under data/.
recorder-supervisor.log
recorder.pid

# Research digests written by crew/research_crew.py. Machine-local output,
# regenerated daily by the timer.
reports/
